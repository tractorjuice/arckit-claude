---
type: file_exists
path: projects/000-global/ARC-000-PRIN-v1.0.md
---

The command writes the global principles artefact at the document-ID path. The `validate-arc-filename` hook rewrites a near-miss, so a file anywhere else means the model wrote outside the convention entirely.
