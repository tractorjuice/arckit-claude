---
type: tool_used
tool: Write
min: 0
max: 0
---

`/arckit:search` declares `doc-type: none`. It reads the index the hook injected and reports; it writes nothing. Write and Edit are deliberately in `allowed_tools` so the grader measures restraint, not permission.
